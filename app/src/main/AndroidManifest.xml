<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.emotiontrack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ChangeStatusPopWindow"></activity>
        <activity android:name=".ChangeStatus" />
        <activity android:name=".SharePopWindow" />
        <activity android:name=".ShareInput" />
        <activity
            android:name=".SavePopWindow"
            android:theme="@style/AppTheme.PopWindowTheme" /> <!-- change the activity style -->
        <activity android:name=".Analysis" />
        <activity android:name=".BuzzTipsOverview" />
        <activity android:name=".SelfReflectSave" />
        <activity android:name=".BuzzTipsChoose" />
        <activity android:name=".SelfReflectChoose" />
        <activity
            android:name=".DashBoard"
            android:label="@string/title_activity_dash_board" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>